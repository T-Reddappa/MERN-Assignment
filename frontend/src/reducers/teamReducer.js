import React from "react";

const initialState = {
  team: [],
};

const teamReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default teamReducer;
